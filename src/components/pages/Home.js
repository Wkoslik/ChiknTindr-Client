const Home = (props) => {
    return (
        <div>
            <h1>HOMEPAGE</h1>
            <p>user click <a href="/auth/login">here</a> to login and start</p>
        </div>
    )
}

export default Home